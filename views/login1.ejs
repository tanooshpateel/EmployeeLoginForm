<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Login Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #2c3e50; /* Updated background color */
            color: #fff; /* Text color for better contrast */
        }

        .container {
            display: flex;
            height: 100vh;
            justify-content: center; /* Center the content horizontally */
            align-items: center; /* Center the content vertically */
        }

        .login-form {
            background-color: #34495e; /* Form background color */
            padding: 40px;
            border-radius: 5px;
            width: 400px;
        }

        .form-header {
            margin-top: 0;
            text-align: center;
            font-size: 24px;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        label {
            margin-bottom: 5px;
            font-weight: bold;
        }

        input {
            width: 100%;
            margin-bottom: 20px;
            padding: 12px;
            border: none;
            border-radius: 5px;
            background-color: #ecf0f1; /* Input field background color */
            color: #444; /* Input field text color */
            transition: background-color 0.3s;
        }

        input:focus {
            outline: none;
            background-color: #d0d3d4; /* Input field background color on focus */
        }

        button {
            background-color: #007BFF;
            color: #fff;
            border: none;
            padding: 12px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #0056b3;
        }

        .form-text {
            margin-top: 20px;
            color: #bdc3c7; /* Form text color */
            text-align: center;
        }

        a {
            color: #3498db; /* Link color */
            text-decoration: none;
            transition: color 0.3s;
        }

        a:hover {
            color: #2980b9; /* Link color on hover */
        }

        .content {
            text-align: center;
            margin-bottom: 40px;
        }

        .content h2 {
            font-size: 30px;
            margin-bottom: 20px;
        }

        .content p {
            font-size: 18px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="login-form">
            <div class="content">
                <h2>Welcome to Our Website!</h2>
                <p>We provide the best services and solutions for your needs.</p>
                <p>Join us now and explore a world of possibilities!</p>
            </div>
            <h1 class="form-header">Login Form</h1>
            <form action="/login" method="post">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required>
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required>
                <button type="submit">Login</button>
            </form>
            <p class="form-text">Don't have an account? <a href="/signup">Signup here</a></p>
        </div>
    </div>
</body>

</html>
